-- Map definitions: single source of truth. Keys: id, name, trophyReward, unlockAt
local Maps = {}

Maps.List = {
	{ id = "default", name = "Default Arena", trophyReward = 1, unlockAt = 0 },
}

return Maps
